Steps to run the program:

1. To compile the file, go to the bin folder, use ‘javac ../src/*.java’.

2. To run the program, use ‘java Main 8080’ and 8080 is an argument, which is user-defined port number. 
